{
  "routes": [
    {
      "method": "GET",
      "path": "/bullets",
      "handler": "bullet.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/bullets/count",
      "handler": "bullet.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/bullets/:id",
      "handler": "bullet.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/bullets",
      "handler": "bullet.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/bullets/:id",
      "handler": "bullet.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/bullets/:id",
      "handler": "bullet.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
